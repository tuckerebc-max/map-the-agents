# Release v0.0.12

## 🎉 New Features

### TTL/Expiration Support for Worktrees (#67)

Worktrees can now have an expiration time. This helps manage temporary worktrees that are no longer needed after a certain period.

**Usage:**

```bash
# Create a worktree with a 7-day expiration
gwq add --expires 7d github.com/example/repo

# Create a worktree with a 1-hour expiration
gwq add --expires 1h github.com/example/repo

# Prune expired worktrees (dry-run)
gwq prune --expired --dry-run

# Prune expired worktrees
gwq prune --expired

# Force prune expired worktrees (skip confirmation)
gwq prune --expired --force
```

The `internal/duration` package supports flexible duration formats such as `7d`, `1h`, `30m`, etc.

## 🐛 Bug Fixes

- Fixed `prune --expired` to correctly handle manually deleted worktrees (#70)
- Fixed error return value handling of `os.RemoveAll` in tests (#71)

## 📦 Dependencies

- Updated `github.com/bmatcuk/doublestar/v4` to v4.10.0 (#66)

## 📝 Documentation

- Replaced Japanese configuration articles with English ones (#62, thanks @shunk031)
- Added development environment setup documentation
- Removed unmaintained design documentation (#63)

## 🔧 Code Improvements

- Fixed lefthook golangci-lint configuration to use `./...` instead of `{staged_files}` (#64)

## 👥 Contributors

- @shunk031 (#62)
- @tupe12334 (#67, #70)

## 📦 Upgrade Instructions

**Homebrew:**

```bash
brew upgrade d-kuro/tap/gwq
```

**Go:**

```bash
go install github.com/d-kuro/gwq/cmd/gwq@v0.0.12
```

**Full Changelog**: https://github.com/d-kuro/gwq/compare/v0.0.11...v0.0.12
