# Claude Code

@AGENTS.md

## Changelog scope

CI configuration, release/publish pipelines, repository automation, maintainer scripts, and agent-instruction changes are infrastructure-level changes—not user-facing package changes. Do not add them to `packages/*/CHANGELOG.md` unless they also change shipped package behavior, APIs, features, or user-visible fixes.
