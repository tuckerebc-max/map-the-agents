# MonkeyAI 管理后台

MonkeyAI 管理后台负责单实例内的用户身份、AI 资源、会话用量、积分计费与管理审计。系统不承担多租户能力，所有数据天然属于当前实例。

## 系统与资源

**系统**:
一套独立部署并服务固定用户集合的 MonkeyAI 实例，是配置、数据和权限的最高边界。
_避免_: 团队、租户、组织

**用户**:
可登录当前系统的人员身份，同时包含管理角色、启停状态和个人资料；当前管理页面称其为“成员”。
_避免_: 成员、员工、团队成员

**分组**:
系统内从唯一根分组展开、按树形结构组织用户的授权和额度继承单元；选择父分组时包含其所有后代分组。
_避免_: 部门、角色

**管理员分组**:
当前角色为管理员的用户集合，是根分组下由系统维护的固定分组。
_避免_: 管理部门、管理员角色

**系统资源**:
由管理员创建和维护、归系统所有并供被授权用户共同使用的模型、规则、技能或 MCP 服务。
_避免_: 团队资源、管理员资源

**个人资源**:
由某个用户创建并持有、可授权给其他用户或分组使用的 AI 资源。
_避免_: 用户资源、私有资源

**全局设置**:
在当前系统中唯一且对所有用户生效的品牌、登录、邮件和计费规则集合。
_避免_: 团队设置、连接配置

**产品名称**:
系统面向用户展示的产品品牌名称。
_避免_: 工具名称、助手名称

## 授权

**资源访问授权**:
将某项资源以指定访问级别分享给一个用户或分组；分组授权天然覆盖其后代分组中的用户。对于系统规则，授权还可以要求目标用户必须应用该规则。
_避免_: 访问范围、权限列表、白名单

**只读访问**:
允许用户查看和使用资源，但不允许修改资源配置或内容。
_避免_: 使用范围、可见范围

**读写访问**:
允许用户查看、使用和修改资源，但不包含分享、删除或转移所有权。
_避免_: 编辑范围、管理权限

**资源所有者**:
持有个人资源、拥有完整资源权限并可以将其分享给其他用户或分组的用户。
_避免_: 创建者、管理员

**规则强制对象**:
必须自动应用某条系统规则且不能自行移除该规则的分组或用户。
_避免_: 强制生效范围、强制权限

## AI 资源

**标签**:
由系统统一维护并可关联到不同类型资源的分类词条。
_避免_: 技能标签

**模型**:
可被会话调用的一项大模型配置，包含协议、上下文能力、凭据和积分倍率。
_避免_: 模型供应商、模型实例

**技能**:
以技能包交付、为 Agent 提供专门工作方法和配套文件的可授权资源。
_避免_: 插件、工具

**规则**:
约束 Agent 行为或输出的文本指令；系统规则可以被强制应用，个人规则不能被管理员强制应用。
_避免_: 提示词、策略

**MCP 服务**:
提供一组可发现工具的远程 MCP 连接配置。
_避免_: 工具、插件

**MCP 工具**:
由 MCP 服务暴露的单个可调用能力，可独立启停和配置每次调用积分。
_避免_: MCP 服务

**专家**:
由角色提示词、MCP 工具、规则和技能组合而成的系统级 Agent 预设。
_避免_: 助手、机器人

## 执行与计费

**会话**:
用户从某个客户端发起、承载连续交互及其模型与工具调用的工作上下文。
_避免_: 任务、请求

**模型调用**:
会话期间向一个模型发起的一次请求，是模型成功率、时延和 Token 用量的事实记录。
_避免_: 模型用量、推理调用

**积分**:
MonkeyAI 对模型和工具用量进行统一计价的内部单位。
_避免_: 金额、Token

**额度**:
每个刷新周期分配给分组或用户的积分上限；未单独设置时从最近上级分组继承。
_避免_: 余额、预算

**积分账户**:
记录用户在当前额度周期内可用积分余额的账户。
_避免_: 额度、钱包

**计费流水**:
积分账户每次扣减、发放、退款、重置或人工调整形成的不可变记录。
_避免_: 用量、账单
