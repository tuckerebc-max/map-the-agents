# 贡献指南 (Contributing Guide)

感谢你有兴趣为**若爱 (IfAI)** 项目做出贡献！我们欢迎所有形式的贡献,无论是报告 Bug、提出功能建议、改进文档,还是提交代码。

---

## 📋 目录

- [行为准则](#行为准则)
- [如何贡献](#如何贡献)
  - [报告 Bug](#报告-bug)
  - [功能建议](#功能建议)
  - [提交代码](#提交代码)
  - [改进文档](#改进文档)
- [开发环境设置](#开发环境设置)
- [代码规范](#代码规范)
- [提交规范](#提交规范)
- [Pull Request 流程](#pull-request-流程)
- [测试指南](#测试指南)
- [社区与沟通](#社区与沟通)

---

## 行为准则

参与本项目的所有贡献者都应遵守以下原则:

- **友善和尊重** - 尊重不同的观点和经验
- **包容多样性** - 欢迎所有背景的贡献者
- **专注建设性讨论** - 批评应该针对想法,而不是针对个人
- **协作精神** - 我们共同构建更好的工具

不可接受的行为包括但不限于:
- 使用性暗示语言或图像
- 人身攻击或侮辱性评论
- 骚扰行为(公开或私下)
- 未经许可发布他人的隐私信息
- 其他在专业环境中不适当的行为

违反行为准则的参与者可能会被暂时或永久禁止参与项目。

---

## 如何贡献

### 报告 Bug

发现 Bug 是对项目的重要贡献！在提交 Bug 报告前,请:

1. **检查是否已存在** - 搜索 [Issues](https://github.com/peterfei/ifai/issues) 确认问题未被报告
2. **确认是 Bug** - 确保问题可复现,不是操作失误
3. **提供详细信息** - 使用 Bug 报告模板,包含:
   - 问题描述和预期行为
   - 复现步骤 (越详细越好)
   - 系统环境 (操作系统、版本号等)
   - 错误日志和截图
   - 可能的解决方案 (如果有)

**Bug 报告模板:**

```markdown
### 问题描述
[简洁描述问题]

### 复现步骤
1. 打开应用
2. 点击 '...'
3. 滚动到 '...'
4. 看到错误

### 预期行为
[应该发生什么]

### 实际行为
[实际发生了什么]

### 环境信息
- OS: [e.g. macOS 14.0, Windows 11, Ubuntu 22.04]
- IfAI 版本: [e.g. 0.1.0]
- Node.js 版本: [e.g. 18.17.0]
- Rust 版本: [e.g. 1.75.0]

### 错误日志
[粘贴相关日志]

### 截图
[如有必要,添加截图]
```

### 功能建议

我们欢迎新功能的建议！提交功能请求时:

1. **描述问题** - 解释你遇到的痛点
2. **提出解决方案** - 描述你期望的功能
3. **列举备选方案** - 考虑过哪些其他方法
4. **说明价值** - 这个功能对哪些用户有帮助

**功能建议模板:**

```markdown
### 问题背景
[描述你想解决的问题]

### 建议的解决方案
[详细描述期望的功能]

### 备选方案
[考虑过的其他实现方式]

### 额外上下文
[其他有用的信息、截图、参考]

### 优先级
[ ] 低 - 锦上添花
[ ] 中 - 重要但不紧急
[ ] 高 - 非常需要
```

### 提交代码

代码贡献是项目发展的核心！在开始编码前:

1. **沟通优先** - 在 Issue 中讨论你的想法
2. **小步前进** - 将大功能拆分为多个 PR
3. **关注质量** - 代码清晰比聪明更重要
4. **添加测试** - 确保你的代码有测试覆盖

详细步骤见 [Pull Request 流程](#pull-request-流程)。

### 改进文档

文档同样重要！你可以帮助:

- 修复错别字和语法错误
- 改进不清晰的说明
- 添加使用示例
- 翻译成其他语言
- 补充缺失的内容

文档贡献遵循相同的 PR 流程。

---

## 开发环境设置

### 前置要求

确保已安装:

- **Node.js** >= 18.0 ([下载](https://nodejs.org/))
- **Rust** >= 1.70 ([通过 rustup 安装](https://rustup.rs/))
- **Git** ([下载](https://git-scm.com/))
- **系统依赖**:
  - **Windows**: [Visual Studio Build Tools](https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022)
  - **macOS**: `xcode-select --install`
  - **Linux**: `sudo apt install build-essential libgtk-3-dev libwebkit2gtk-4.0-dev`

### 克隆仓库

```bash
# 通过 HTTPS 克隆
git clone https://github.com/peterfei/ifai.git
cd ifai

# 或通过 SSH 克隆 (需要配置 SSH key)
git clone git@github.com:peterfei/ifai.git
cd ifai
```

### 安装依赖

```bash
# 安装 Node.js 依赖
npm install

# Rust 依赖会在构建时自动安装
```

### 启动开发服务器

```bash
# 启动 Tauri 开发模式 (前端 + 后端)
npm run tauri dev

# 或仅启动前端开发服务器
npm run dev
```

应用会自动编译并启动,通常在 10-30 秒内完成。

### 项目结构

```
ifainew/
├── src/                    # React 前端代码
│   ├── components/        # UI 组件
│   ├── stores/           # Zustand 状态管理
│   ├── utils/            # 工具函数
│   ├── i18n/             # 国际化配置
│   └── App.tsx           # 应用入口
│
├── src-tauri/             # Rust 后端代码
│   ├── src/
│   │   ├── lib.rs        # 主入口
│   │   ├── file_walker.rs
│   │   ├── terminal.rs
│   │   ├── git.rs
│   │   └── ...
│   ├── Cargo.toml
│   └── tauri.conf.json   # Tauri 配置
│
├── tests/                 # 测试用例
│   └── spec_*.cjs
│
├── public/                # 静态资源
├── .github/               # GitHub 配置
└── package.json
```

---

## 代码规范

### TypeScript / JavaScript

- **使用 TypeScript** - 所有新代码必须使用 TypeScript
- **严格模式** - 启用 `"strict": true`
- **命名规范**:
  - 文件名: `camelCase.ts` 或 `PascalCase.tsx` (组件)
  - 变量/函数: `camelCase`
  - 类/组件: `PascalCase`
  - 常量: `UPPER_SNAKE_CASE`
- **组件规范**:
  - 使用函数组件和 Hooks
  - Props 使用 TypeScript 接口定义
  - 导出类型: `export interface MyComponentProps { ... }`
- **注释**:
  - 复杂逻辑添加注释
  - 公共 API 使用 JSDoc 注释

**示例:**

```typescript
// ✅ 好的写法
interface FileTreeProps {
  rootPath: string;
  onFileSelect: (path: string) => void;
}

export const FileTree: React.FC<FileTreeProps> = ({ rootPath, onFileSelect }) => {
  const [expanded, setExpanded] = useState<Set<string>>(new Set());

  // 处理文件夹展开/折叠
  const handleToggle = useCallback((path: string) => {
    setExpanded(prev => {
      const next = new Set(prev);
      next.has(path) ? next.delete(path) : next.add(path);
      return next;
    });
  }, []);

  return (
    <div className="file-tree">
      {/* 渲染逻辑 */}
    </div>
  );
};

// ❌ 避免的写法
export const FileTree = (props: any) => {  // 不要使用 any
  let expanded = [];  // 应该使用 useState

  function handleToggle(path) {  // 缺少类型
    // ...
  }

  return <div>{/* ... */}</div>;
};
```

### Rust

- **遵循 Rust 官方规范** - 运行 `cargo fmt` 和 `cargo clippy`
- **命名规范**:
  - 文件名: `snake_case.rs`
  - 函数/变量: `snake_case`
  - 类型/Trait: `PascalCase`
  - 常量: `UPPER_SNAKE_CASE`
- **错误处理**:
  - 使用 `Result<T, E>` 而不是 `panic!`
  - 公共 API 返回明确的错误类型
- **注释**:
  - 公共 API 使用 `///` 文档注释
  - 模块使用 `//!` 注释

**示例:**

```rust
// ✅ 好的写法
use serde::{Deserialize, Serialize};
use tauri::command;

#[derive(Debug, Serialize, Deserialize)]
pub struct FileInfo {
    pub path: String,
    pub size: u64,
    pub modified: u64,
}

/// 获取指定目录下的所有文件信息
///
/// # 参数
/// * `root_path` - 根目录路径
///
/// # 返回
/// * `Ok(Vec<FileInfo>)` - 文件信息列表
/// * `Err(String)` - 错误描述
#[command]
pub async fn get_files(root_path: String) -> Result<Vec<FileInfo>, String> {
    std::fs::read_dir(&root_path)
        .map_err(|e| format!("读取目录失败: {}", e))?
        .filter_map(|entry| entry.ok())
        .map(|entry| {
            // 处理逻辑
            Ok(FileInfo { /* ... */ })
        })
        .collect()
}

// ❌ 避免的写法
#[command]
pub fn get_files(path: String) -> Vec<FileInfo> {
    let entries = std::fs::read_dir(&path).unwrap();  // 不要使用 unwrap
    // ...
}
```

### CSS / TailwindCSS

- **优先使用 Tailwind 类** - 减少自定义 CSS
- **语义化类名** - 如需自定义 CSS,使用有意义的类名
- **响应式设计** - 使用 Tailwind 的响应式前缀 (`sm:`, `md:`, `lg:`)
- **深色模式** - 使用 `dark:` 前缀支持深色主题

---

## 提交规范

我们遵循 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/) 规范:

### 格式

```
<类型>(<范围>): <描述>

[可选的正文]

[可选的脚注]
```

### 类型 (Type)

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 仅文档变更
- `style`: 代码格式 (不影响功能)
- `refactor`: 重构 (既不是新功能也不是修复)
- `perf`: 性能优化
- `test`: 添加或修复测试
- `chore`: 构建过程或辅助工具的变动
- `ci`: CI 配置变更

### 范围 (Scope) - 可选

- `editor`: Monaco 编辑器相关
- `chat`: AI 对话功能
- `terminal`: 终端功能
- `git`: Git 集成
- `lsp`: LSP 客户端
- `ui`: 用户界面
- `core`: 核心功能
- `build`: 构建系统

### 示例

```bash
# 新功能
feat(editor): 添加多光标编辑支持

# Bug 修复
fix(chat): 修复流式响应中断问题

修复了在网络不稳定时,AI 响应流可能意外中断的问题。
现在会自动重试并保持连接。

Fixes #123

# 文档更新
docs(readme): 更新快速开始指南

# 性能优化
perf(editor): 优化大文件打开速度

通过延迟加载和虚拟滚动,将 10MB+ 文件的打开时间从 5s 降低到 500ms。
```

---

## Pull Request 流程

### 1. Fork 仓库

点击 GitHub 页面右上角的 "Fork" 按钮。

### 2. 克隆你的 Fork

```bash
git clone https://github.com/YOUR_USERNAME/ifai.git
cd ifai
```

### 3. 创建分支

```bash
# 从 main 分支创建新分支
git checkout -b feature/my-awesome-feature

# 或
git checkout -b fix/issue-123
```

分支命名规范:
- `feature/xxx` - 新功能
- `fix/xxx` - Bug 修复
- `docs/xxx` - 文档更新
- `refactor/xxx` - 重构

### 4. 进行开发

```bash
# 编写代码
# ...

# 添加和提交更改
git add .
git commit -m "feat(editor): 添加新功能"

# 继续开发
# ...
git commit -m "test(editor): 添加单元测试"
```

### 5. 运行测试

```bash
# 运行测试套件
npm test

# 或运行特定测试
node tests/spec_agent_flow.cjs

# 检查代码格式 (Rust)
cd src-tauri
cargo fmt --check
cargo clippy -- -D warnings
```

### 6. 推送到 GitHub

```bash
git push origin feature/my-awesome-feature
```

### 7. 创建 Pull Request

1. 访问你的 Fork 页面
2. 点击 "New Pull Request"
3. 选择 base: `main` ← compare: `feature/my-awesome-feature`
4. 填写 PR 描述:

**PR 模板:**

```markdown
## 变更描述
[简要描述这个 PR 做了什么]

## 变更类型
- [ ] Bug 修复 (非破坏性变更,修复问题)
- [ ] 新功能 (非破坏性变更,添加功能)
- [ ] 破坏性变更 (会导致现有功能不按预期工作的修复或功能)
- [ ] 文档更新

## 关联 Issue
Closes #[issue 编号]

## 测试方式
[描述如何测试你的更改]

## 截图 (如适用)
[添加截图帮助解释你的更改]

## 检查清单
- [ ] 我的代码遵循项目的代码规范
- [ ] 我已进行自我审查
- [ ] 我已添加必要的注释 (特别是复杂逻辑)
- [ ] 我的更改不会产生新的警告
- [ ] 我已添加测试证明我的修复有效或功能正常工作
- [ ] 新增和现有的单元测试在本地通过
- [ ] 我已更新相关文档
```

### 8. 代码审查

- 维护者会审查你的 PR
- 根据反馈进行修改:
  ```bash
  # 进行修改
  git add .
  git commit -m "fix: 根据审查意见修改"
  git push origin feature/my-awesome-feature
  ```
- PR 会自动更新

### 9. 合并

审查通过后,维护者会合并你的 PR。恭喜,你已成为贡献者！

---

## 测试指南

### 运行测试

```bash
# 运行所有测试
npm test

# 运行特定测试
node tests/spec_agent_flow.cjs

# 运行 Rust 测试
cd src-tauri
cargo test
```

### 编写测试

#### 前端测试 (未来)

目前项目还没有前端单元测试,但计划使用:
- Vitest - 测试框架
- React Testing Library - 组件测试

#### Spec 测试 (Node.js)

Spec 测试用于验证核心功能:

```javascript
// tests/spec_my_feature.cjs

const assert = require('assert');

// 测试用例
function testMyFeature() {
  const result = myFunction('input');
  assert.strictEqual(result, 'expected', 'Result should match expected');
}

// 运行测试
try {
  testMyFeature();
  console.log('✅ Test Passed!');
} catch (error) {
  console.error('❌ Test Failed:', error.message);
  process.exit(1);
}
```

#### Rust 测试

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_file_walker() {
        let result = walk_directory("/tmp");
        assert!(result.is_ok());
        assert!(!result.unwrap().is_empty());
    }

    #[tokio::test]
    async fn test_async_function() {
        let result = async_function().await;
        assert_eq!(result, expected_value);
    }
}
```

---

## 社区与沟通

### GitHub Discussions

对于一般性讨论、问题和想法分享:
👉 [https://github.com/peterfei/ifai/discussions](https://github.com/peterfei/ifai/discussions)

### GitHub Issues

对于 Bug 报告和功能请求:
👉 [https://github.com/peterfei/ifai/issues](https://github.com/peterfei/ifai/issues)

### 响应时间

- Bug 报告: 通常在 24-48 小时内回复
- 功能请求: 通常在 3-7 天内回复
- Pull Request: 通常在 2-5 天内审查

### 维护者

- [@peterfei](https://github.com/peterfei) - 项目创始人

---

## 认可贡献者

所有贡献者都会被记录在项目的贡献者列表中。我们非常感谢每一位贡献者的付出！

---

## 许可

通过向本项目提交代码,你同意你的贡献将按照 [MIT License](./LICENSE) 进行许可。

---

## 问题?

如果你有任何问题,请随时:
- 在 [GitHub Discussions](https://github.com/peterfei/ifai/discussions) 中提问
- 创建一个 Issue

我们很乐意帮助你开始贡献！

---

**再次感谢你为若爱 (IfAI) 做出贡献！** 🎉
