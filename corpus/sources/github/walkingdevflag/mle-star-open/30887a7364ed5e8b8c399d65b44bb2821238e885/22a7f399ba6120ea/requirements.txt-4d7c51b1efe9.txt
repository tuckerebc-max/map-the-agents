# Requirements derived from pyproject.toml (Poetry) and code imports
# Target: Python 3.12
# Note: PyTorch wheels are platform-specific. This pins a stable CPU build range; adjust if you need GPU/optimized builds.

# Core client/runtime (OpenAI-compatible, no Google deps)
openai
httpx
python-dotenv
pydantic
duckduckgo-search

# ML stack
numpy
pandas
scikit-learn
# PyTorch (platform-specific wheels). Leave unpinned for cross-platform install; pin if your environment requires it.
torch

# Optional CLIs
absl-py

# Test dependencies
pytest
pytest-asyncio
